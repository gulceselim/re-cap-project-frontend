import { FilterCarPipePipe } from './filter-car-pipe.pipe';

describe('FilterCarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
