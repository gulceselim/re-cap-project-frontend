import { FilterCarDetailPipePipe } from './filter-car-detail-pipe.pipe';

describe('FilterCarDetailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarDetailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
